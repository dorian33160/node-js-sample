version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable  # Image de base CircleCI
    steps:
      - checkout  # Récupère le code source

      - run:
          name: Build Docker image
          command: |
            docker build -t mon-app-node .

      - run:
          name: Vérifier l'image Docker
          command: |
            docker images

workflows:
  version: 2
  build-and-push:
    jobs:
      - build
